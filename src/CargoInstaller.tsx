import ActivityIndicator from './components/ActivityIndicator';

const CargoInstaller = ({}) => {
	return (
		<ActivityIndicator message={"Checking for Cargo installation"} />
	);
};

export default CargoInstaller;